const getPlayers = async () => {
    try {

    } catch (error) {
        throw error;
    }
}

const getTime = async () => {
    try {

    } catch (error) {
        throw error;
    }
}

const postTime = async () => {
    try {

    } catch (error) {
        throw error;
    }
}

module.exports = {
    getPlayers,
    getTime,
    postTime,
}